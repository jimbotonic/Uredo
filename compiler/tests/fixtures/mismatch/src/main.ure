##! Negative fixture: a type error on a call whose argument Uredo elaborated (§27).

struct Job:
    id: u32

fn describe(job: Job) -> String:
    format("job {}", job.id)

fn main():
    job = Job { id: 1 }
    n: i32 = describe(job)           # E0308: a String is not an i32
    print("{n}")
