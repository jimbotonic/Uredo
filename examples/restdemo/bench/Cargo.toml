[package]
name = "restdemo_bench"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "The load generator for examples/restdemo, so the comparison is reproducible"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "net", "macros", "io-util", "time"] }

[profile.release]
lto = true
codegen-units = 1

[workspace]
