[package]
name = "compat_nostd"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[workspace]
